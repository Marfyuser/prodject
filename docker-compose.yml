version: '3.8'
services:
  nginx:
    image: nginx:alpine
    container_name: local-nginx
    ports:
      - "8080:80"
    volumes:
      - ./nginx-site/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx-site/:/usr/share/nginx/html/:ro
    restart: unless-stopped 